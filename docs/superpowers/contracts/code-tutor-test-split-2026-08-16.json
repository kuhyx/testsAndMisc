{
  "title": "Clear the seven over-cap code_tutor test modules",
  "objective": "Bring every python_pkg/code_tutor test module under the repo-wide 250-line cap by partitioning them at the banner comments that already mark each function-under-test, taking code_tutor from thirteen violations to zero.",
  "acceptance_criteria": [
    "bash ~/utils/scripts/check_file_length.sh --all lists no file under python_pkg/code_tutor.",
    "The code_tutor suite still reports exactly 217 passing tests -- the count established once the cli lint fix added its covering test -- proving no test was dropped or silently skipped.",
    "The package keeps 100% branch coverage of the code_tutor source modules.",
    "No patch string changes: this is a pure partition of test sections, and the source splits in the preceding commits already put every patched name in the module each file names.",
    "The shared _make_verifier builder moves to tests/conftest.py rather than being duplicated, and the local copy of _make_live_mock that shadowed conftest's is deleted.",
    "No assertion is changed and no test is deleted, skipped or weakened.",
    "No suppression is added and no coverage exclusion is introduced."
  ],
  "out_of_scope": [
    "Changing what any test asserts.",
    "Touching the code_tutor source, which is already under the cap.",
    "Adding a fourth partN-style file name: each new module is named after the concern it covers."
  ],
  "verifier": "timeout 120 env PYTHONPATH=. python3 -m pytest python_pkg/code_tutor/tests -q (expects exactly 217 passed), plus bash ~/utils/scripts/check_file_length.sh --all showing no code_tutor entry, plus pre-commit run --files on the touched files."
}
