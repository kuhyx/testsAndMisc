{
  "intent": "betting-sim's Flutter app gets a launcher icon from the shared family instead of the stock Flutter beach icon.",
  "scope": [
    "python_pkg/app_icons/apps.py — one new AppIcon entry",
    "Non-goal: no change to the generator, the glyph set, or any other app's icon"
  ],
  "changes": [
    "Registered betting_sim: repo ~/betting-sim/app, accent #B8862E, glyph track-bars (rising bars with a plotted trend point, which reads as odds movement).",
    "Set linux=false: betting-sim's desktop target is a Chrome-wrapped web build rather than the GTK embedder, so the hicolor PNG set would go unread."
  ],
  "verification": [
    {
      "command": "PYTHONPATH=. python3 -m python_pkg.app_icons list",
      "result": "pass",
      "evidence": "betting_sim  #B8862E  track-bars  /home/kuhy/betting-sim/app"
    },
    {
      "command": "PYTHONPATH=. python3 -m python_pkg.app_icons generate --app betting_sim --android",
      "result": "pass",
      "evidence": "wrote 6 asset files to /home/kuhy/betting-sim/app/assets/icon; mipmaps generated after adding flutter_launcher_icons to the app pubspec"
    },
    {
      "command": "visual check of assets/icon/icon.png",
      "result": "pass",
      "evidence": "charcoal #211D1B field, #B8862E glyph — on-family, no stock beach icon"
    }
  ],
  "risks": [
    "None to existing apps: the change is purely additive, one tuple entry.",
    "If betting-sim later grows a real GTK Linux target, linux must flip to true or the hicolor set will be missing."
  ],
  "rollback": [
    "Remove the betting_sim AppIcon entry from python_pkg/app_icons/apps.py.",
    "Re-run `app_icons list` and confirm the other 14 apps are unchanged."
  ]
}
