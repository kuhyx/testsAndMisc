# Run the safety-net unit whenever the G29 is (re)enumerated, so a hotplug is
# covered as well as boot. Installed by fixes/fix_g29_shifter.sh.
ACTION=="add", SUBSYSTEM=="hid", ENV{MODALIAS}=="hid:b0003g*v0000046Dp0000C24F", TAG+="systemd", ENV{SYSTEMD_WANTS}+="g29-shifter-bpf.service"
